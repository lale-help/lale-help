h2= t('.header')

= render partial: 'list', locals: { circle: current_circle, working_group: nil, tasks: @tasks.open, title: t('.volunteer-header')}

= render partial: 'list', locals: { circle: current_circle, working_group: nil, tasks: @tasks.open_org, title: t('.organizer-header')}
